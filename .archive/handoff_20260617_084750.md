# Handoff — Archon adversarial workflows + cross-model "different mind" loop

**Date:** 2026-06-17 08:47
**Primary repo:** `C:\dev\repos\Archon` (workflow YAMLs + engine spec). Runs targeted `C:\dev\projects\cGantt` (GitHub `przemek64/cGantt`).
**Execution target:** Windows-native Archon via `bun /c/dev/repos/Archon/packages/cli/src/cli.ts` (NOT WSL). Run pattern:
`cd /c/dev/projects/<slug> && ARCHON_SUPPRESS_NESTED_CLAUDE_WARNING=1 bun /c/dev/repos/Archon/packages/cli/src/cli.ts workflow run <name> --branch <b> --from <base> "<msg>"` (use `run_in_background`).
Discovery only scans `.archon/workflows/defaults/` recursively — drop new workflow YAMLs THERE or `workflow run` reports "not found".

## What this session did

1. Ran adversarial fix-issue workflows against **cGantt issue #26** (a backward resource-constrained cook scheduler). Issue was **simplified ~2x** mid-session to fit Kimi's context window (Kimi 400 `context window exceeds limit (2013)` on the original).
2. Built/edited workflows in `C:\dev\repos\Archon\.archon\workflows\defaults\`:
   - `archon-fix-github-issue-mixed-aggressive.yaml` — copy of `-adversarial-mixed` with `threshold`/`passThreshold` = **9**.
   - `archon-fix-github-issue-mixed-progressive.yaml` — **capped-ratchet**: stage 1 baseline (all ≥7), then stage 2 hardening to per-criterion `min(baselineScore+2,10)` with anti-regression. New state fields (`stage`, `round`, `stageRetry`, `bestSeen`, `hardenTarget`); `max_iterations` 24. Max ~8 rounds / 16 model passes.
   - Moved review-judgment nodes to **codex/gpt-5.5** in `archon-comprehensive-pr-review.yaml` (6 nodes), `archon-smart-pr-review.yaml` (7), `archon-validate-pr.yaml` (3). Mechanical/E2E nodes left on pi/minimax.
3. Confirmed verdict logic: pass = score **≥** threshold (a 7 passes at default). Per-criterion `threshold` is LLM-generated in the `negotiate-criteria` node (template default 7) → lands in each run's `contracts/impl.json`.
4. **Validated the cross-model loop feature** (user implemented `loop.roleModels`): ran `archon-fix-github-issue-adv2-mixed` on #26. Logs show `loop_node.role_model_selected` alternating `building→pi/kimi`, `evaluating→codex/gpt-5.5`. Feature is live in source (`schemas/loop.ts` `roleModelsSchema`; `dag-executor.ts` ~2044-2172). CLI runs from `src`, so no restart needed for CLI path.

## Key finding (the important result)

On cGantt #26, **same-model** adversarial runs gave FALSE PASSES; the **cross-model** run did not:

- PR **#27** (`-mixed`, kimi eval) and PR **#28** (`mixed-aggressive`, kimi eval): each passed round-0 with 9s/10s, **but both ship an identical dependency-ordering bug** (a step starts before its `after:` predecessor finishes). #27 also sprawled into unrelated `app.js`/`audio.js`/`server.js` (~127 lines off-task); #28 stayed surgical.
- PR **#29** (`adv2-mixed`, codex eval): codex **failed round-0** (scored a fake capacity test 6/10), forced a rebuild; round-1 passed; final code is **correct**.
- Independent verifier confirms: #27/#28 FAIL the dependency case, **#29 passes ALL checks**.

Conclusion: cross-model (different-mind) evaluation caught what same-model self-grading missed. `fresh_context` alone (same model) was insufficient.

## Important artifacts (do not duplicate — read these)

- Engine spec for per-role models: `C:\dev\repos\Archon\docs\specs\loop-per-role-model.md` (now implemented; spec still accurate as design doc).
- Independent scheduler verifier (NOT in any repo — recreate if needed): was at `/tmp/assess.js`. Checks dependency order, per-second capacity, finish-anchoring, integer offsets, determinism/immutability, deadline+capacity infeasibility. Run: `node /tmp/assess.js <path-to-scheduler.js>`. **Consider committing this into cGantt as `tests/scheduler.contract.test.js`** (see Option 1 below).
- PRs: cGantt #27, #28 (buggy, same-model), #29 (correct, cross-model). Issue: `przemek64/cGantt#26` (currently the simplified body).
- Run artifacts: `C:\Users\admin\.archon\workspaces\przemek64\cGantt\artifacts\runs\<run-id>\{contracts/impl.json,feedback/round-*.json,impl-state.json}`. adv2 run = `4f4642847cbe7c2adcd94e8a2f19afab`.

## Open threads / next steps

- **Option 1 (per-project pinned test):** add the independent verifier as a mandatory test in cGantt so the contract's `test-suite-green` criterion can't be gamed. NOTE: universal/hygiene checks alone would NOT have caught the dependency bug (code built, suite green, tests existed) — only a per-project behavioral test does. The scope-guard check WOULD have caught #27's sprawl.
- **Option 2 (gate node):** add a post-loop gate that runs fixed tests and blocks PR (labeltool already does this via `.archon/gates.json` `project-gate`). Universal hygiene checks (build/type-check green, no regressions, a test was added, scope guard, git hygiene) belong here; domain checks belong per-project.
- Decide whether to retrofit `roleModels` (codex evaluator) into `-mixed`/`-aggressive`/`-progressive`, or keep `adv2-*` as the cross-model line.
- `mixed-progressive` has NOT been run yet — untested on a real issue.
- Mirror Archon changes into WSL source `/home/user/archon-src` if WSL execution is ever used (never edit the compiled binary).
- The original (hard) #26 body is gone (overwritten by simplified). Recoverable from git PR diffs / earlier conversation if the hard version is wanted again.

## Gotchas observed

- Kimi (`kimi-coding/kimi-for-coding`) has a small context window — fails on large issues/contracts with HTTP 400 `(2013)`. Also saw a transient HTTP 500 `999 (1000)`.
- Claude/opus plan nodes hit the 5-hour usage cap earlier; the mixed/codex variants avoid Claude entirely.
- Background-run output files: `C:\Users\admin\AppData\Local\Temp\claude\...\tasks\<id>.output`. Python on Windows needs `C:/...` paths, not `/c/...`.

## Suggested skills for the next agent

- `archon` — for any further workflow create/run/config work (this whole session is in its wheelhouse).
- `verify` — to confirm a PR's code actually works (e.g. re-run the scheduler verifier against #29 or a new run).
- `code-review` — to review PR #29's diff before merge, or compare #27/#28/#29.
- `handoff` — to refresh this doc after the next chunk of work.
